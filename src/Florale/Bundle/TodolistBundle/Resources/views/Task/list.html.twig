{% extends '::base.html.twig' %}

{% datagrid_theme datagrid _self %}

{% block stylesheets %}
    {{ parent() }}
    {{ datagrid_stylesheets(datagrid) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ datagrid_javascripts(datagrid) }}
{% endblock %}

{% block body %}
    <div class="container">
        <h1>task list</h1>
        <a class="btn btn-default" href="{{ path('task_new', {'tasklist': tasklist}) }}" onclick="">
            <i class="icon-plus-sign-alt"></i> {{ "task.button.new"|trans }}
        </a>
        {{ datagrid(datagrid) }}
    </div>
{% endblock %}
